🥈 High Impact
4. Live System Stats Panel
Show: CPU %, RAM used, Frames processed/sec, Storage used / free. Proves edge deployment viability — that's the "lowband" value prop.

5. Compression Ratio Display
Show "Original: 12.4 MB → Compressed: 2.1 MB (83% saved)" per clip. The zstd+7z pipeline is already doing this — just surface the numbers.

6. Demo Mode / Simulation Button
A button that injects a fake "loitering detected" event with animation. Judges at hackathons often can't trigger real events — this saves your demo from going flat.
